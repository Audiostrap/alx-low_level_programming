#include "main.h"

/**
 * _puts -  function that prints a string,
 * followed by a new line, to stdout
 * @str: an integer pointer
 */
void _puts(char *str)
{
	int i;

	while (i = 0)
	{
		_putchar(str);
		i++;
	}
	_putchar('\n');
}
